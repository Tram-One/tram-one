const { raw } = require('@nx-js/observer-util');
/**
 * @name raw
 * @link https://tram-one.io/#raw
 * @description
 * Function that which takes in an observable object, and returns the raw value.
 * This is intended for copying values
 *
 * @param observable
 *
 * @returns plain javascript object with no observability
 */
export default raw;
