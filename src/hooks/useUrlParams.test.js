const useUrlParams = require('./useUrlParams')

// tests can be found in the use-url-params project
// see https://github.com/Tram-One/useUrlParams

describe('useUrlParams', () => {
	it('should be defined', () => {
		expect(useUrlParams).toBeDefined()
	})
})
